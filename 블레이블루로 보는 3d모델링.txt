블레이블루로 보는 3d모델링

엔진에서 지원하는 그림자는 색감이 이쁘지 않아서 그냥 텍스쳐로 그려넣음
캐릭터 모션 및 자연스러운 표정변화를 위해서 born을 엄청 넣음 (부하up)
캐릭마다 개별조명을 지원 (부하up)
애니메이션 같은 그래픽을 위해서 대전화면에서 보이는 얼굴의 모델링과 컷씬에서의 얼굴 모델링이 차이가 있음(줌아웃 상황에서 이목구비 확대)
같은 이유로 캐릭터 그림자의 대부분을 날림(임계값을 만들어서 임계값을 넘으면 그림자 생기게 X)
애니메이션 스러운 그림자 표현을 위해 대부분 수작업으로 그림자를 넣거나 뺌

텍스쳐 채널이 굉장히 다양화되어있음(자원 크게 안먹음)

그림자 셋을 여려개 만들어서 맵에따라서 다른 그림자 셋을 넣음

외곽선효과(캐릭터 윤곽을 떠서 그림자 스트라이트를 만들어서 조금 더 크게 만들어서 후면에 배치) - 배면법

애니메이션같은 효과(선 굵기)를 위해서 선을 잘게잘게 분할해서 각각 굵기값을 따로 주었음(쌉 노가다)

모토무라 라인(만든 사람의 이름을 따서 지음) 애니메이션틱한 선 표현을위해 모델링 중간중간을 파버림

극적인 원근감을 표현하기위해 건물이나 배경을 눌러 비틀어서 표현함

머리카락의 변형이 큰 캐릭의 경우 필요한 형태에따라 여러 모델링을 만들어두었다가 상황에 맞게 변경함

연출씬의 경우 모델링을 비틀어서 표현함으로써 조금더 만화적인 표현을 구현함

이펙트도 3d로 했음(변태색기들)

3d게임의 경우 측면에 위치하면 앵글때문에 왜곡이생김 이를 위해서 어느지점에 위치하게되면 클라이언트 내에서 직접적으로 모델링을 왜곡시키면서 플레이어가 보기에 왜곡이 안생긴 것 처럼 보이게 만듦

z버퍼 : 캐릭터들이 겹칠때, 어느쪽을 우선적으로 표시할지 우선도에 따라(개발자가 정함) 표시하게됨


